interface:
  display_name: "Temper"
  short_description: "Repo-specific patterns and workflows for temper"
  default_prompt: "Use the temper repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true